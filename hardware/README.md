# Altium Source Files

The original Altium source project was not included in the supplied package.

Add the following files here before describing the repository as fully reproducible:

```text
hardware/
├── dac-automation-hat.PrjPcb
├── dac-automation-hat.SchDoc
├── dac-automation-hat.PcbDoc
├── libraries/
└── outputs/
```

A schematic PDF should also be exported to `docs/` for reviewers who do not use Altium Designer.
